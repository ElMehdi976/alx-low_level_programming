#include<stdio.h>
/**
  *main- Entry point
  *Return: always 0
  */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (m = 98; m <= 102; m++)
	{
		putchar(m);
	}
	return (0);
}
